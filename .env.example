# Server Configuration
NODE_ENV=
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=email_api
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=

# Data Retention (in hours)
DATA_RETENTION_HOURS=48

# CORS Configuration (use * to allow all origins, or comma-separated list)
ALLOWED_ORIGINS=*

# Optional: Enable automatic data cleanup (only for non-Lambda deployments)
# ENABLE_DATA_CLEANUP=false